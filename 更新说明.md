# DeepResearch-Agent 更新说明

> 版本：v0.2.0 ｜ 日期：2026-08-09 ｜ 分支：`codex/reviewer-multi-persona`

本次更新围绕《调研与优化报告-2026-08-09.md》完成了从"演示级"到"生产可用"的加固与功能补齐，并修复了测试中发现的若干问题。全量自动化测试 **385 通过 / 2 跳过 / 0 失败**。

---

## ✨ 新增功能

### 研究与报告质量
- **引用核验后处理**：报告末尾自动追加「引用核验」章节，标注无法访问的来源 URL。
- **论断-证据接地核查**：报告末尾自动生成「论断-证据核验」表（Claims-Evidence），无来源支撑的论断被显式标注。
- **STORM 风格多视角规划**：Planner 先输出 3-5 个研究视角，再按视角拆解子任务；Writer 写入视角上下文。
- **三角色批判评审**：Reviewer 由"怀疑派实践者 / 对抗性评审 / 实现工程师"并行评审，取最严分数并合并反馈。
- **多轮迭代参数真实生效**：前端"研究深度"（快速/标准/深度）真正控制评审迭代轮数。
- **当前日期注入**：四个 Agent 提示词自动携带当天日期，避免按训练截止时间做旧年份假设。

### 并发与可靠性
- **多任务并行 + 实时切换**：可同时发起多个研究任务，侧栏"运行中"列表随时切回任一任务的实时 Agent 轨迹与报告流。
- **事件任务级隔离**：并发任务的事件/SSE 流互不串扰。
- **断点续跑（可选）**：LangGraph Checkpointer 持久化；默认关闭自动恢复，避免启动时静默消耗 token，可通过 `RESUME_INTERRUPTED_TASKS=true` 开启。
- **任务状态全量落库**：`research_data`、`sources`、`total_tokens` 持久化到数据库。

### 性能与成本
- **子任务有界并行**：默认同时执行 3 个子任务（`RESEARCH_PARALLELISM`）。
- **上下文预算压缩**：Writer 上下文受 `CONTEXT_MAX_CHARS`（默认 60,000 字）约束，控制 token 消耗。
- **角色模型路由**：Planner/Researcher/Writer/Reviewer 可分别配置模型（`LLM_MODEL_*`）。
- **LLM 客户端复用 + token 成本统计**：每次研究记录各模型 token 消耗。
- **可选 Langfuse 追踪**：`LANGFUSE_ENABLED=true` 时记录每次 LLM 调用。

### 安全
- **analyze 工具默认禁用**，开启后封锁 `os`/`subprocess`/`socket` 等危险模块（`ENABLE_PYTHON_TOOL`）。
- **生产安全校验**：开启认证时必须设置非默认 `JWT_SECRET`，否则启动即报错。
- **`.env.*` 加入 gitignore**，防止密钥误提交。

### 工程与体验
- **一键启动脚本**：双击启动，自动补环境/依赖/前端构建，端口冲突自动切换（8000→8010→8080→8090），服务器就绪后才打开浏览器。
- **浏览器抓取升级**：Playwright 优先（支持 JS 渲染与 PDF 文本提取），失败自动回退 httpx。
- **搜索后端可插拔**：Tavily/DuckDuckGo/GitHub/Exa 注册表化，改 `SEARCH_BACKENDS` 即可切换。
- **历史分页真实生效 + PDF 下载缓存**。
- **评测闭环**：`tests/eval/run_eval.py`（LLM-as-Judge + 固定题集）可量化报告质量。
- **ARQ worker（可选）**：`app/worker.py` 支持将长任务执行与 API 进程解耦（需 Redis）。

---

## 🐛 修复

- PDF 下载 500：报告中的 markdown 表格（论断-证据核验）导致 reportlab 解析失败，已新增表格渲染与异常兜底。
- 历史列表只显示 5 条：后端 `/api/history` 默认每页 5 条，已改为默认 100 条/页。
- 搜索失败时不再注入 mock 假数据，mock 来源永不进入引用列表。
- 并发任务事件串线（全局回调改为任务级 contextvar）。
- 删除旧 ResearchService 死代码与重复代码块。
- `/api/history` 分页参数此前未生效，现已真实分页。

---

## 📋 测试情况

- 自动化测试：385 通过 / 2 跳过 / 0 失败（详见 `测试评估报告-2026-08-09.md`）。
- 真实环境验证：API 功能 18/18 通过；双任务并发 + SSE 隔离验证通过；报告含引用核验与论断-证据表；MD/PDF 下载正常。
- 已知注意事项：
  - 本机 8000 端口与另一项目 Job Copilot 冲突，一键启动脚本默认使用 8010。
  - 单个课题约 5-7 分钟（取决于子任务数量），可调 `RESEARCH_PARALLELISM` 加速。

---

## 📚 相关文档

- 调研与优化报告：`调研与优化报告-2026-08-09.md`
- 实施计划：`docs/superpowers/plans/2026-08-09-architecture-hardening-and-optimization.md`
- 测试评估报告：`测试评估报告-2026-08-09.md`
