# DeepResearch-Agent · 工程级设计文档

> 基于多 Agent 协作的自动化研究分析系统
>
> 版本：v1.0 | 最后更新：2026-07-19

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术选型与依据](#2-技术选型与依据)
3. [系统架构](#3-系统架构)
4. [模块详述](#4-模块详述)
5. [数据流设计](#5-数据流设计)
6. [API 设计](#6-api-设计)
7. [12 天实施计划](#7-12-天实施计划)
8. [验收标准](#8-验收标准)
9. [附录](#9-附录)

---

## 1. 项目概述

### 1.1 定位

DeepResearch-Agent 是一个基于多 Agent 协作架构的自动化研究分析系统。用户输入研究课题，系统自动完成：任务拆解 → 资料搜索 → 网页深度阅读 → 知识整理 → 报告撰写 → 质量审查的完整闭环。

### 1.2 核心能力矩阵

| 能力 | 说明 | 面试亮点 |
|------|------|----------|
| Agent Workflow | 多 Agent 编排与状态管理 | LangGraph 有状态图 |
| Planning | 任务自动拆解与依赖规划 | LLM-as-Planner |
| Tool Calling | 多工具动态路由与调用 | 函数调用范式 |
| Browser Automation | 网页内容获取与交互 | Playwright 集成 |
| RAG | 外部知识检索增强 | Embedding + VectorDB |
| Memory | 会话与长时记忆管理 | 双存储策略 |
| Multi-Agent | 分工协作架构 | Planner→Researcher→Writer |
| Report Generation | 结构化报告输出 | Markdown/PDF 双格式 |
| Reflection | 自动质量评估与修正 | Reviewer 迭代回路 |

### 1.3 对比说明

| 维度 | DeepResearch-Agent | ChatGPT 搜索 | 竞品（如 Perplexity） |
|------|-------------------|-------------|---------------------|
| 架构 | 多 Agent 协作 | 单轮 RAG | 搜索+摘要 |
| 深度 | 多步推理+多源交叉 | 单次检索 | 搜索+单次总结 |
| 可控性 | 中间结果可观测 | 黑盒 | 有限 |
| 可扩展 | 插件式 Tool 系统 | 封闭 | 封闭 |

---

## 2. 技术选型与依据

### 2.1 技术栈总览

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| 语言 | Python | ≥3.11 | LLM 生态最佳支持 |
| Web 框架 | FastAPI | ≥0.110 | 异步原生、自动 OpenAPI |
| Agent 编排 | LangGraph | ≥0.2 | 有状态图、支持循环/条件边 |
| LLM SDK | LangChain-OpenAI / Anthropic SDK | latest | 统一接口 |
| 浏览器 | Playwright | latest | 完善的异步 API |
| 向量数据库 | ChromaDB | ≥0.5 | 轻量、嵌入式中使用 |
| 内存缓存 | Redis（或 fakeredis 开发用） | ≥7.0 | 会话管理与缓存 |
| 前端 | React 18 + Tailwind CSS + Vite | latest | 轻量快速开发 |
| 报告生成 | WeasyPrint / markdown + wkhtmltopdf | - | Markdown→PDF 转换 |
| 容器化 | Docker + docker-compose | - | 一键部署 |
| SSE | FastAPI SSE (sse-starlette) | latest | 实时流式输出 |

### 2.2 关键依赖（requirements.txt）

```
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sse-starlette>=2.0.0
langgraph>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.3.0
chromadb>=0.5.0
redis>=5.0.0  # 开发可用 fakeredis 替代
playwright>=1.40.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
pydantic>=2.5.0
weasyprint>=61.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
```

### 2.3 AI 模型策略

| 角色 | 推荐模型 | 选用原因 |
|------|----------|----------|
| Planner | Claude Sonnet 4.5 / GPT-4o | 强规划能力 |
| Researcher | Claude Haiku 4.5 / GPT-4o-mini | 速度快、成本低 |
| Writer | Claude Sonnet 4.5 / GPT-4o | 高质量输出 |
| Reviewer | Claude Sonnet 4.5 / GPT-4o | 严谨判断 |

---

## 3. 系统架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      User (浏览器 / API)                      │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP / SSE
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                     FastAPI 服务层                            │
│   POST /api/research  │  GET /api/research/{id}/stream       │
│   GET /api/reports/{id} │  POST /api/knowledge/ingest       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   LangGraph Workflow Engine                   │
│                                                              │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐ │
│  │  Router  │──▶│ Planner  │──▶│Researcher│──▶│  Writer  │ │
│  │  Agent   │   │  Agent   │   │  Agent   │   │  Agent   │ │
│  └──────────┘   └──────────┘   └──────────┘   └────┬─────┘ │
│                                                     │       │
│                                                     ▼       │
│                                              ┌──────────┐   │
│                                              │ Reviewer │   │
│                                              │  Agent   │   │
│                                              └──────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    Tool 路由层                                 │
│                                                              │
│   ┌─────────────┐  ┌─────────────┐  ┌──────────────────┐    │
│   │ Search Tool  │  │Browser Tool │  │  Python Tool     │    │
│   │ (Web Search) │  │(Playwright) │  │(Code Executor)   │    │
│   └─────────────┘  └─────────────┘  └──────────────────┘    │
│                                                              │
│   ┌──────────────────┐  ┌────────────────────────────────┐   │
│   │    RAG Tool       │  │     Memory Tool                │   │
│   │(ChromaDB检索增强) │  │  (Session + Knowledge Memory) │   │
│   └──────────────────┘  └────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                     数据持久层                                 │
│                                                              │
│    ┌──────────┐    ┌──────────┐    ┌──────────────────┐     │
│    │  Redis   │    │ ChromaDB │    │   文件系统         │     │
│    │(会话/缓存)│    │(向量存储) │    │(报告/论文PDF)   │     │
│    └──────────┘    └──────────┘    └──────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 LangGraph 工作流定义

```
State: ResearchState
├── task: str                    # 用户原始任务
├── plan: List[SubTask]          # Planner 输出的子任务列表
├── current_step: int            # 当前执行步骤
├── research_data: List[Document]# 收集的研究资料
├── report_draft: str            # 报告草稿
├── review_score: float          # 审查评分
├── review_feedback: str         # 审查反馈
├── final_report: str            # 最终报告
├── errors: List[str]            # 错误记录
└── status: str                  # running | completed | failed

Workflow 节点:
1. router_node     → 判断是否开始/继续/结束
2. planner_node    → 任务拆解（仅首次执行）
3. researcher_node → 执行搜索+阅读（循环执行直到完成所有子任务）
4. writer_node     → 生成报告
5. reviewer_node   → 审查质量
6. report_formatter→ 格式化输出

Workflow 边:
- START → router_node
- router_node → planner_node (条件: 首次)
- router_node → researcher_node (条件: 有计划且未完成)
- router_node → END (条件: 出错或完成)
- planner_node → researcher_node
- researcher_node → router_node (条件: 还有子任务)
- researcher_node → writer_node (条件: 所有子任务完成)
- writer_node → reviewer_node
- reviewer_node → router_node (条件: 评分低于阈值，重回 writer)
- reviewer_node → report_formatter (条件: 评分达标)
- report_formatter → END
```

### 3.3 项目目录结构

```
deep-research-agent/
├── .env                          # 环境变量（API KEY 等）
├── .env.example                  # 环境变量模板
├── CLAUDE.md                     # Karpathy 编码指南
├── README.md                     # 项目 README
├── docker-compose.yml            # 容器编排
├── Dockerfile                    # 服务镜像
├── requirements.txt              # 依赖清单
├── pyproject.toml                # 项目元数据
│
├── app/
│   ├── __init__.py
│   ├── main.py                   # FastAPI 入口
│   ├── config.py                 # 配置管理（pydantic-settings）
│   ├── models/                   # 数据模型
│   │   ├── __init__.py
│   │   ├── state.py              # ResearchState (LangGraph State)
│   │   ├── schemas.py            # API 请求/响应模型
│   │   ├── report.py             # 报告模型
│   │   └── tools.py              # Tool 调用/返回模型
│   │
│   ├── workflow/                 # LangGraph 工作流
│   │   ├── __init__.py
│   │   ├── graph.py              # 图定义与编译
│   │   ├── nodes.py              # 所有节点函数
│   │   └── conditions.py         # 条件边判断函数
│   │
│   ├── agents/                   # Agent 实现
│   │   ├── __init__.py
│   │   ├── base.py               # BaseAgent（LLM 调用模板）
│   │   ├── planner.py            # PlannerAgent
│   │   ├── researcher.py         # ResearcherAgent
│   │   ├── writer.py             # WriterAgent
│   │   └── reviewer.py           # ReviewerAgent
│   │
│   ├── tools/                    # Tool 实现
│   │   ├── __init__.py
│   │   ├── base.py               # BaseTool 接口
│   │   ├── search.py             # 搜索工具
│   │   ├── browser.py            # 浏览器工具
│   │   ├── python_executor.py    # Python 执行工具
│   │   ├── rag_retriever.py      # RAG 检索工具
│   │   └── memory.py             # 记忆工具
│   │
│   ├── services/                 # 业务服务层
│   │   ├── __init__.py
│   │   ├── research_service.py   # 研究任务编排
│   │   ├── report_service.py     # 报告生成服务
│   │   └── memory_service.py     # 记忆管理服务
│   │
│   ├── rag/                      # RAG 系统
│   │   ├── __init__.py
│   │   ├── document_loader.py    # 文档加载器（PDF/MD/HTML）
│   │   ├── chunker.py            # 文本分块
│   │   ├── embedder.py           # 向量化
│   │   ├── vector_store.py       # 向量存储封装
│   │   └── retriever.py          # 检索器
│   │
│   ├── memory/                   # 记忆系统
│   │   ├── __init__.py
│   │   ├── session_memory.py     # 会话记忆（Redis）
│   │   └── knowledge_memory.py   # 知识记忆（ChromaDB）
│   │
│   ├── web/                      # 前端
│   │   ├── index.html
│   │   ├── src/
│   │   │   ├── App.jsx
│   │   │   ├── main.jsx
│   │   │   ├── components/
│   │   │   │   ├── InputPanel.jsx
│   │   │   │   ├── AgentTrace.jsx      # 实时 Agent 轨迹
│   │   │   │   ├── ReportViewer.jsx
│   │   │   │   └── HistoryList.jsx
│   │   │   └── hooks/
│   │   │       └── useSSE.js            # SSE 连接 Hook
│   │   └── package.json
│   │
│   └── utils/                    # 工具函数
│       ├── __init__.py
│       ├── llm.py                # LLM 调用封装
│       ├── markdown_utils.py     # Markdown 处理
│       └── pdf_utils.py          # PDF 生成
│
├── data/                         # 数据目录
│   ├── reports/                  # 生成的报告
│   ├── knowledge/                # 知识库文档
│   ├── chroma_db/                # ChromaDB 持久化
│   └── uploads/                  # 用户上传
│
└── tests/                        # 测试
    ├── __init__.py
    ├── test_workflow.py
    ├── test_tools.py
    ├── test_agents.py
    └── conftest.py
```

---

## 4. 模块详述

### 4.1 FastAPI 服务层 (app/main.py)

**职责：** HTTP 接口暴露、请求校验、SSE 流式推送、并发任务管理。

**路由设计：**

| 方法 | 路径 | 功能 | 输入 | 输出 |
|------|------|------|------|------|
| POST | /api/research | 发起研究任务 | `{task: str, options?: {...}}` | `{task_id: str}` |
| GET | /api/research/{task_id}/stream | SSE 实时流 | path param | `text/event-stream` |
| GET | /api/research/{task_id} | 查询任务状态 | path param | `{status, report, ...}` |
| GET | /api/reports/{task_id} | 获取报告文件 | path param | PDF/Markdown |
| POST | /api/knowledge/ingest | 导入知识文档 | `multipart/form-data` | `{doc_id: str}` |
| GET | /api/knowledge/search | 知识库检索 | `?q=xxx` | `[{doc, score}]` |
| GET | /api/history | 查询历史任务 | - | `[{task_id, task, status, time}]` |

**SSE 事件格式：**

```
event: agent_status
data: {"agent": "planner", "status": "running", "message": "正在分析任务并制定研究计划..."}

event: agent_result
data: {"agent": "planner", "result": {"plan": [...]}}

event: tool_call
data: {"tool": "search", "query": "AI Agent 2026 trend", "status": "running"}

event: tool_result
data: {"tool": "search", "count": 5}

event: report_chunk
data: {"content": "## 1. 行业概述\n2026年..."}

event: error
data: {"message": "搜索服务超时"}

event: completed
data: {"task_id": "xxx", "report_id": "xxx"}
```

### 4.2 LangGraph 工作流引擎 (app/workflow/)

#### 4.2.1 State 定义 (app/models/state.py)

```python
from typing import List, Optional
from pydantic import BaseModel, Field

class SubTask(BaseModel):
    id: str
    description: str
    tool: str  # search | browse | analyze
    status: str = "pending"  # pending | running | completed | failed
    result: Optional[str] = None

class ResearchState(BaseModel):
    task: str
    plan: List[SubTask] = []
    current_step: int = 0
    research_data: List[dict] = []
    report_draft: str = ""
    review_score: float = 0.0
    review_feedback: str = ""
    final_report: str = ""
    errors: List[str] = []
    status: str = "pending"
    iteration_count: int = 0  # reviewer 迭代计数器
```

#### 4.2.2 节点函数 (app/workflow/nodes.py)

```python
def router_node(state: ResearchState) -> dict:
    """判断工作流下一步走向"""
    if state.status == "failed":
        return {"next": "END"}
    if not state.plan:  # 首次运行
        return {"next": "planner"}
    if state.current_step < len(state.plan):
        return {"next": "researcher"}
    if not state.report_draft:
        return {"next": "writer"}
    if state.review_score < 0.7 and state.iteration_count < 3:
        return {"next": "writer"}  # 重写
    if state.review_score >= 0.7:
        return {"next": "formatter"}
    return {"next": "END"}

def planner_node(state: ResearchState, llm) -> dict:
    """任务拆解"""
    prompt = f"""分析研究任务，将其拆解为具体可执行的子任务。
任务：{state.task}

返回 JSON 格式子任务列表：
[{{
  "id": "1",
  "description": "具体任务描述",
  "tool": "search|browse|analyze"
}}]

要求：
- 3-6 个子任务
- 每个子任务指定一种工具
- 描述要具体可执行"""
    # ... 调用 LLM 解析结果
```

### 4.3 Agent 实现 (app/agents/)

#### 4.3.1 BaseAgent (app/agents/base.py)

```python
class BaseAgent(ABC):
    """所有 Agent 的基类，统一 LLM 调用模板"""
    model_config = {
        "temperature": 0.3,
        "max_tokens": 4096,
    }

    def __init__(self, model_name: str = "claude-sonnet-4-5"):
        self.llm = self._init_llm(model_name)

    @abstractmethod
    def system_prompt(self) -> str:
        """每个 Agent 定义自己的系统提示词"""
        ...

    async def invoke(self, state: ResearchState, tools: dict) -> dict:
        """调用 LLM，传入当前状态和可用工具"""
        messages = [
            {"role": "system", "content": self.system_prompt()},
            {"role": "user", "content": self._format_input(state)}
        ]
        response = await self.llm.ainvoke(messages)
        return self._parse_response(response)
```

#### 4.3.2 PlannerAgent

**职责：** 接收用户原始任务，拆解为可执行的子任务列表。

**系统提示词要点：**
- 你是研究规划专家
- 将复杂问题分解为 3-6 个步骤
- 每个步骤明确指定工具类型
- 输出必须是结构化 JSON

**输出示例：**
```json
[
  {"id": "1", "description": "搜索2026年AI Agent行业市场规模和增长数据", "tool": "search"},
  {"id": "2", "description": "搜索主要玩家（OpenAI、Anthropic、Google等）最新动态", "tool": "search"},
  {"id": "3", "description": "深入阅读2-3篇关键分析报告", "tool": "browse"},
  {"id": "4", "description": "分析搜索数据，提取核心趋势和结论", "tool": "analyze"}
]
```

#### 4.3.3 ResearcherAgent

**职责：** 执行子任务，调用 Tool 系统搜集资料，整理结构化知识。

**流程：**
1. 获取当前子任务描述
2. 根据 tool 类型调用对应 Tool
3. 处理 Tool 返回结果，提取关键信息
4. 将结果追加到 state.research_data
5. 标记子任务为 completed

#### 4.3.4 WriterAgent

**职责：** 根据 research_data 中的全部资料，生成结构化报告。

**报告模板：**

```markdown
# {报告标题}

## 摘要
{200-300字摘要}

## 1. 研究背景
{背景介绍}

## 2. 核心发现
{逐条列出关键发现，每点附数据来源}

## 3. 详细分析
### 3.1 {子标题}
{分析内容，引用来源}

## 4. 趋势与展望
{趋势判断}

## 5. 结论与建议
{结论}

## 参考来源
{自动生成的引用列表}
```

#### 4.3.5 ReviewerAgent

**职责：** 对报告草稿进行质量评估。

**评估维度：**

| 维度 | 权重 | 说明 |
|------|------|------|
| 信息完整性 | 30% | 是否覆盖了所有子任务的内容 |
| 引用质量 | 25% | 是否有来源引用，来源是否可靠 |
| 逻辑连贯性 | 25% | 论证是否清晰，结构是否合理 |
| 深度分析 | 20% | 是否超出表面信息，有独立见解 |

**阈值策略：**
- 评分 ≥ 0.85：直接通过
- 评分 0.70 - 0.84：反馈问题，重写一次
- 评分 < 0.70：详细反馈，最多重写 2 次
- 超过 3 次迭代：强制输出当前版本

### 4.4 Tool 系统 (app/tools/)

#### 4.4.1 BaseTool 接口

```python
class BaseTool(ABC):
    name: str
    description: str
    parameters: dict  # JSON Schema

    @abstractmethod
    async def execute(self, **kwargs) -> ToolResult:
        ...
```

#### 4.4.2 SearchTool

```python
class SearchTool(BaseTool):
    name = "web_search"
    description = "搜索互联网获取最新信息"

    async def execute(self, query: str, max_results: int = 5) -> ToolResult:
        """使用 Web Search API（如 Tavily / SerpAPI / Bing）"""
        results = await search_api.search(query, max_results=max_results)
        return ToolResult(
            data=[{
                "title": r.title,
                "url": r.url,
                "snippet": r.snippet
            } for r in results]
        )
```

#### 4.4.3 BrowserTool

```python
class BrowserTool(BaseTool):
    name = "web_browser"
    description = "打开网页提取正文内容"

    async def execute(self, url: str) -> ToolResult:
        """使用 Playwright 打开页面，提取标题和正文"""
        async with async_playwright() as p:
            browser = await p.chromium.launch()
            page = await browser.new_page()
            await page.goto(url, wait_until="networkidle")
            title = await page.title()
            # 提取正文（去除导航/广告/页脚）
            content = await page.evaluate("""() => {
                const article = document.querySelector('article');
                if (article) return article.innerText;
                const main = document.querySelector('main');
                if (main) return main.innerText;
                return document.body.innerText;
            }""")
            await browser.close()
        return ToolResult(data={
            "title": title,
            "content": content[:8000],  # 截断长内容
            "url": url
        })
```

#### 4.4.4 PythonTool

```python
class PythonTool(BaseTool):
    name = "python_executor"
    description = "执行 Python 代码进行数据分析"

    async def execute(self, code: str, timeout: int = 30) -> ToolResult:
        """在沙箱中执行 Python 代码"""
        # 使用 RestrictedPython 或临时 Docker 容器执行
        result = await self._run_in_sandbox(code, timeout)
        return ToolResult(data=result)
```

#### 4.4.5 Tool 路由机制

```python
# app/tools/router.py
TOOL_REGISTRY = {
    "search": SearchTool(),
    "browse": BrowserTool(),
    "analyze": PythonTool(),
    "rag_retrieve": RAGRetrieverTool(),
    "memory": MemoryTool(),
}

class ToolRouter:
    def get_tool(self, name: str) -> BaseTool:
        tool = TOOL_REGISTRY.get(name)
        if not tool:
            raise ToolNotFoundException(f"Tool '{name}' not found")
        return tool

    async def execute(self, name: str, params: dict) -> ToolResult:
        tool = self.get_tool(name)
        return await tool.execute(**params)
```

### 4.5 RAG 系统 (app/rag/)

#### 4.5.1 数据处理流水线

```
文档源（PDF / 技术博客 / GitHub README）
        │
        ▼
DocumentLoader（PyMuPDF for PDF, BeautifulSoup for HTML）
        │
        ▼
Chunker（RecursiveCharacterTextSplitter，chunk_size=512, overlap=64）
        │
        ▼
Embedder（text-embedding-3-small / voyage-2）
        │
        ▼
ChromaDB（collection: "research_knowledge"）
        │
        ▼
Retriever（similarity_search with score threshold 0.75）
```

#### 4.5.2 检索增强策略

```python
class RAGRetriever:
    def __init__(self, collection_name="research_knowledge"):
        self.client = chromadb.PersistentClient(path="./data/chroma_db")
        self.collection = self.client.get_or_create_collection(
            name=collection_name,
            embedding_function=OpenAIEmbeddings()
        )

    async def add_documents(self, documents: List[Document]):
        chunks = self.chunker.split_documents(documents)
        texts = [chunk.page_content for chunk in chunks]
        metadatas = [chunk.metadata for chunk in chunks]
        ids = [str(uuid.uuid4()) for _ in chunks]
        self.collection.add(documents=texts, metadatas=metadatas, ids=ids)

    async def retrieve(self, query: str, k: int = 5) -> List[Document]:
        results = self.collection.query(
            query_texts=[query],
            n_results=k,
            include=["documents", "metadatas", "distances"]
        )
        return self._format_results(results)
```

### 4.6 Memory 系统 (app/memory/)

#### 4.6.1 双存储策略

| 类型 | 存储 | 内容 | 生命周期 |
|------|------|------|----------|
| Session Memory | Redis | 当前任务上下文、执行状态、中间结果 | 任务完成后 24h 过期 |
| Knowledge Memory | ChromaDB | 历史报告、研究资料、知识片段 | 持久化，可检索 |

#### 4.6.2 SessionMemory

```python
class SessionMemory:
    def __init__(self, redis_url="redis://localhost:6379"):
        self.redis = redis.from_url(redis_url)

    async def save_state(self, task_id: str, state: ResearchState):
        await self.redis.setex(
            f"session:{task_id}",
            86400,  # 24h TTL
            state.model_dump_json()
        )

    async def load_state(self, task_id: str) -> ResearchState:
        data = await self.redis.get(f"session:{task_id}")
        return ResearchState.model_validate_json(data)
```

#### 4.6.3 KnowledgeMemory

```python
class KnowledgeMemory:
    """保存历史研究中积累的知识"""

    async def save_report(self, task: str, report: str):
        """将完整报告存入知识库"""
        doc = Document(page_content=report, metadata={"task": task, "type": "report"})
        await self.vector_store.add_documents([doc])

    async def query_similar(self, query: str, k: int = 3) -> List[Document]:
        """检索与当前任务相关的历史研究"""
        return await self.retriever.aretrieve(query, k=k)
```

### 4.7 前端 (app/web/)

**技术栈：** React 18 + Tailwind CSS + Vite

**核心组件：**

```
<App>
  ├── <Header />                     # Logo + 标题
  ├── <InputPanel />                 # 研究任务输入框 + 启动按钮
  ├── <AgentTrace />                 # Agent 执行轨迹（实时 SSE）
  │   ├── <AgentStatusCard />        # 每个 Agent 的状态卡片
  │   ├── <ToolCallLog />            # Tool 调用日志
  │   └── <ProgressBar />            # 整体进度条
  ├── <ReportViewer />               # 报告展示（Markdown 渲染）
  │   ├── <MarkdownRenderer />       # Markdown → HTML
  │   ├── <CitationPanel />          # 引用来源侧边栏
  │   └── <ExportButtons />          # 下载 Markdown / PDF
  └── <HistoryList />                # 历史任务列表
```

**SSE 连接 Hook (hooks/useSSE.js)：**

```javascript
export function useSSE(taskId) {
  const [events, setEvents] = useState([]);

  useEffect(() => {
    const source = new EventSource(`/api/research/${taskId}/stream`);

    source.addEventListener('agent_status', (e) => {
      setEvents(prev => [...prev, JSON.parse(e.data)]);
    });

    source.addEventListener('tool_call', (e) => {
      setEvents(prev => [...prev, JSON.parse(e.data)]);
    });

    source.addEventListener('report_chunk', (e) => {
      setEvents(prev => {
        const last = prev[prev.length - 1];
        if (last?.type === 'report') {
          last.content += JSON.parse(e.data).content;
          return [...prev];
        }
        return [...prev, { type: 'report', content: JSON.parse(e.data).content }];
      });
    });

    source.addEventListener('completed', () => source.close());
    return () => source.close();
  }, [taskId]);

  return events;
}
```

---

## 5. 数据流设计

### 5.1 一次完整研究任务的数据流

```
用户输入: "分析 2026 年 AI Agent 行业趋势"

Step 1: POST /api/research → { task_id: "res_001" }
  ↓ 返回 task_id，开始后台执行

Step 2: 浏览器 GET /api/research/res_001/stream （SSE 连接）
  ↓

Step 3: Router Node
  ↓ plan 为空 → 进入 Planner

Step 4: Planner Agent
  ↓ 调用 LLM → 输出子任务列表
  ↓ state.plan = [子任务1, 子任务2, ...]
  ↓ SSE: agent_status → { agent: "planner", status: "completed", plan: [...] }
  ↓

Step 5: Researcher Agent 循环
  ↓ 取子任务1 → tool=search
  ↓ SearchTool.execute("2026 AI Agent 市场规模")
  ↓ SSE: tool_call → { tool: "search", query: "..." }
  ↓ 返回搜索结果 → 存入 state.research_data
  ↓ SSE: tool_result → { tool: "search", count: 5 }
  ↓
  ↓ 取子任务2 → tool=browse
  ↓ BrowserTool.execute("https://example.com/report")
  ↓ SSE: tool_call → { tool: "browse", url: "..." }
  ↓ 返回页面内容 → 存入 state.research_data
  ↓
  ↓ ... 循环直到所有子任务完成
  ↓

Step 6: Writer Agent
  ↓ 汇总 research_data → 调用 LLM 生成报告
  ↓ SSE: report_chunk → { content: "..." }  (实时流式输出)
  ↓ state.report_draft = 完整报告
  ↓

Step 7: Reviewer Agent
  ↓ 评估报告质量
  ↓ 评分 0.82 < 0.85 → 反馈: "缺少具体数据支撑"
  ↓ SSE: agent_status → { agent: "reviewer", score: 0.82, feedback: "..." }
  ↓

Step 8: Writer Agent (重写)
  ↓ 根据 Reviewer 反馈优化报告
  ↓ SSE: report_chunk → { content: "..." }
  ↓

Step 9: Reviewer Agent (二次审查)
  ↓ 评分 0.91 → 通过
  ↓

Step 10: Report Formatter
  ↓ 生成 Markdown + PDF 文件
  ↓ 存入 data/reports/res_001/
  ↓ SSE: completed → { task_id: "res_001", report_id: "rp_001" }

总耗时: ~2-5 分钟（取决于任务复杂度）
```

### 5.2 错误处理策略

| 场景 | 处理方式 |
|------|----------|
| LLM 调用超时 | 重试 1 次，若失败则跳过该步骤并记录错误 |
| 搜索 API 不可用 | 切换备选搜索源，或返回缓存结果 |
| 浏览器页面加载失败 | 重试 1 次不同 URL，跳过该来源 |
| Python 执行超时 | 截断输出，标记为部分成功 |
| Reviewer 迭代超过上限 | 强制输出当前版本，标记"未完全审查" |

---

## 6. API 设计

### 6.1 发起研究任务

```
POST /api/research

Request:
{
  "task": "分析 2026 年 AI Agent 行业趋势，并生成投资分析报告",
  "options": {
    "depth": "standard",     // quick | standard | deep
    "format": "markdown",    // markdown | pdf
    "knowledge_base": true   // 是否启用知识库增强
  }
}

Response (201):
{
  "task_id": "res_001",
  "status": "pending",
  "stream_url": "/api/research/res_001/stream",
  "created_at": "2026-07-19T10:00:00Z"
}
```

### 6.2 SSE 实时流

```
GET /api/research/{task_id}/stream

Response: text/event-stream
```

### 6.3 查询任务状态

```
GET /api/research/{task_id}

Response:
{
  "task_id": "res_001",
  "status": "completed",       // pending | running | completed | failed
  "task": "分析 2026 年 AI Agent 行业趋势",
  "plan": [...],
  "progress": {
    "total": 5,
    "completed": 5,
    "current": "报告生成完成"
  },
  "report_id": "rp_001",
  "created_at": "...",
  "completed_at": "...",
  "errors": []
}
```

### 6.4 获取报告文件

```
GET /api/reports/{task_id}?format=markdown
GET /api/reports/{task_id}?format=pdf

Response: application/octet-stream (文件下载)
```

---

## 7. 12 天实施计划

### 7.1 甘特图

```
任务 Day1 Day2 Day3 Day4 Day5 Day6 Day7 Day8 Day9 Day10 Day11 Day12
项目初始化    ██
Agent Runtime ██
Tool 系统       ██  ██
Multi-Agent          ██
Memory                  ██
RAG                       ██
Reflection                  ██
报告生成                       ██
前端                              ██  ██
工程优化                                  ██
README+Demo                                    ██
简历准备                                            ██
```

### 7.2 各 Day 交付物与验收标准

---

#### Day 1：项目初始化 + Agent Runtime

**目标：** 跑通 Planner → Executor → Answer 的最小闭环。

**待办事宜：**
- [ ] 初始化项目结构，配置 pyproject.toml
- [ ] 安装所有依赖
- [ ] 实现 ResearchState 模型
- [ ] 实现 LangGraph 基础图（router + planner + executor 三个节点）
- [ ] 实现 LLM 调用封装（app/utils/llm.py）
- [ ] 实现最小 FastAPI 入口（仅 /health 端点）
- [x] 追加 CLAUDE.md（Karpathy 编码指南）

**验收标准：**
```
curl http://localhost:8000/health
→ {"status": "ok", "version": "1.0.0"}
```
测试脚本验证 Planner 能产出合理的子任务列表。

**产出文件：**
- `app/main.py`, `app/models/state.py`
- `app/workflow/graph.py`, `app/workflow/nodes.py`
- `app/agents/base.py`, `app/agents/planner.py`
- `app/utils/llm.py`, `app/config.py`

---

#### Day 2-3：Tool 系统

**目标：** 实现三大核心 Tool，可通过 ToolRouter 动态调用。

**待办事宜：**
- [ ] 实现 BaseTool 抽象类和 ToolRouter
- [ ] 实现 SearchTool（封装 Tavily/SerpAPI）
- [ ] 实现 BrowserTool（Playwright 封装）
- [ ] 实现 PythonTool（沙箱执行）
- [ ] 编写单元测试

**验收标准：**
```python
result = await ToolRouter().execute("search", {"query": "AI Agent 2026"})
assert len(result.data) > 0

result = await ToolRouter().execute("browse", {"url": "https://example.com"})
assert "title" in result.data
```

**产出文件：**
- `app/tools/base.py`, `app/tools/router.py`
- `app/tools/search.py`, `app/tools/browser.py`, `app/tools/python_executor.py`

---

#### Day 4：Multi-Agent

**目标：** 实现 Planner → Researcher → Writer 三 Agent 串联工作流。

**待办事宜：**
- [ ] 完善 ResearcherAgent（连接 Tool 系统）
- [ ] 实现 WriterAgent（结构化报告生成）
- [ ] 实现 Researcher→Writer 的图边连接
- [ ] 确保完整工作流可运行

**验收标准：**
```python
state = ResearchState(task="2026年AI Agent趋势")
executor = LangGraphExecutor(graph)
result = await executor.ainvoke(state)
assert result.status == "completed"
assert len(result.final_report) > 500
```

**产出文件：**
- `app/agents/researcher.py`, `app/agents/writer.py`
- 更新 `app/workflow/nodes.py`, `app/workflow/graph.py`

---

#### Day 5：Memory

**目标：** 实现 Session Memory 和 Knowledge Memory。

**待办事宜：**
- [ ] 配置 Redis（或 fakeredis 开发模式）
- [ ] 实现 SessionMemory（对话上下文保存）
- [ ] 实现 KnowledgeMemory（历史研究保存与检索）
- [ ] 在 Workflow 中集成 Memory

**验收标准：**
```python
memory = SessionMemory()
await memory.save_state("res_001", state)
loaded = await memory.load_state("res_001")
assert loaded.task == state.task
```

**产出文件：**
- `app/memory/session_memory.py`, `app/memory/knowledge_memory.py`
- `app/services/memory_service.py`

---

#### Day 6：RAG

**目标：** 实现知识库构建与检索增强。

**待办事宜：**
- [ ] 实现 DocumentLoader（支持 PDF/MD/HTML）
- [ ] 实现 Chunker（递归分块）
- [ ] 实现 Embedder + ChromaDB 集成
- [ ] 实现 RAG Retriever Tool
- [ ] 加载 3-5 篇预置论文/博客作为初始知识库

**验收标准：**
```python
retriever = RAGRetriever()
await retriever.add_documents([doc])
results = await retriever.retrieve("AI Agent 架构")
assert len(results) > 0
```

**产出文件：**
- `app/rag/document_loader.py`, `app/rag/chunker.py`
- `app/rag/embedder.py`, `app/rag/vector_store.py`, `app/rag/retriever.py`
- `app/tools/rag_retriever.py`

---

#### Day 7：Reflection

**目标：** 实现 Reviewer Agent 的自动质量评估与迭代优化。

**待办事宜：**
- [ ] 实现 ReviewerAgent（多维度评分）
- [ ] 实现 Writer→Reviewer→Writer 循环边
- [ ] 设置迭代上限（3 次）
- [ ] 在 SSE 中暴露评分过程

**验收标准：**
```
一次完整运行中:
1. Writer 输出报告草稿
2. Reviewer 评分 < 0.85 → 反馈 Writer
3. Writer 重写完善
4. Reviewer 评分 ≥ 0.85 或达到迭代上限 → 输出最终报告
```

**产出文件：**
- `app/agents/reviewer.py`
- 更新 `app/workflow/graph.py` 添加循环边

---

#### Day 8：报告生成

**目标：** 支持 Markdown 和 PDF 双格式输出。

**待办事宜：**
- [ ] 实现 ReportFormatter 节点
- [ ] Markdown 格式输出（规范化模板）
- [ ] PDF 格式输出（WeasyPrint / wkhtmltopdf）
- [ ] 引用来源自动整理

**验收标准：**
```
请求 GET /api/reports/res_001?format=pdf
→ 下载合法的 PDF 文件（可打开、内容完整）
```
```
请求 GET /api/reports/res_001?format=markdown
→ 下载规范的 .md 文件
```

**产出文件：**
- `app/utils/markdown_utils.py`, `app/utils/pdf_utils.py`
- `app/services/report_service.py`

---

#### Day 9：前端

**目标：** React 前端 + SSE 实时展示。

**待办事宜：**
- [ ] 初始化 Vite + React + Tailwind 项目
- [ ] 实现 InputPanel 组件
- [ ] 实现 AgentTrace 实时状态组件
- [ ] 实现 ReportViewer Markdown 渲染组件
- [ ] 实现 useSSE hook

**验收标准：**
```
用户在输入框输入任务 → 点击开始 → 页面显示 Agent 执行轨迹（实时更新）
→ 报告逐步渲染 → 完成后可下载
```

**产出文件：**
- `app/web/src/App.jsx`, `app/web/src/main.jsx`
- `app/web/src/components/*.jsx`
- `app/web/src/hooks/useSSE.js`

---

#### Day 10：工程优化

**目标：** SSE 实时推送、Docker 部署、稳定性优化。

**待办事宜：**
- [ ] 实现 SSE 流式推送（sse-starlette）
- [ ] 错误处理与重试机制
- [ ] 日志系统（structlog / loguru）
- [ ] Dockerfile + docker-compose.yml
- [ ] 性能调优（LLM 调用并发、Tool 结果缓存）

**验收标准：**
```
docker-compose up --build
→ 在 localhost:8000 可访问完整应用
→ SSE 流式推送正常
→ 错误场景不会导致服务崩溃
```

**产出文件：**
- `Dockerfile`, `docker-compose.yml`
- 更新 `app/main.py`（SSE 端点）

---

#### Day 11：README + Demo 视频

**目标：** 完善的 README 和高质量的 Demo。

**待办事宜：**
- [ ] 架构图（Mermaid 或 draw.io）
- [ ] README.md（项目简介、安装步骤、使用方法）
- [ ] Demo 脚本（一个完整的端到端演示任务）
- [ ] 录屏 Demo 视频（3-5 分钟）

**README 应包含：**
- 项目名称 + 一句话介绍
- 架构图（核心）
- 功能演示截图/GIF
- 快速开始步骤
- 技术栈列表
- 项目亮点（限 3 个）

---

#### Day 12：简历 + 面试准备

**目标：** 将项目转化为面试竞争力。

**待办事宜：**
- [ ] 修改简历项目描述
- [ ] 准备 5 个常见面试问题的回答
- [ ] 梳理项目难点（技术挑战 + 解决方案）

**常见面试问题准备：**

| 问题 | 回答要点 |
|------|----------|
| 为什么选择 LangGraph 而不是 LangChain Agent？ | 有状态图、更灵活的边条件、支持循环 |
| 如何处理 LLM 幻觉？ | Reviewer Agent + 强制引用来源 + RAG 增强 |
| 多 Agent 之间如何通信？ | LangGraph State 共享机制 |
| 如果搜索 API 不可用怎么办？ | 降级策略：缓存→备用搜索→告知用户 |
| 性能瓶颈在哪里？ | LLM 调用延迟，通过并行 Tool 调用优化 |

---

## 8. 验收标准

### 8.1 功能验收

| # | 验收项 | 预期结果 | 优先级 |
|---|--------|----------|--------|
| F1 | 输入研究任务，系统自动规划 | 3-6 个合理的子任务 | P0 |
| F2 | Agent 依次执行搜索和浏览 | 实时看到 Tool 调用日志 | P0 |
| F3 | 生成结构化 Markdown 报告 | 标题/摘要/分析/来源完整 | P0 |
| F4 | 报告可下载为 PDF | 格式正确的 PDF 文件 | P1 |
| F5 | Reviewer 自动审查报告 | 评分 + 反馈可见 | P1 |
| F6 | SSE 实时展示执行轨迹 | 页面实时更新 | P1 |
| F7 | 知识库检索增强 | 相关结果用于报告 | P2 |
| F8 | 历史任务可追溯 | 已完成的报告可再次查看 | P2 |

### 8.2 非功能验收

| # | 验收项 | 标准 |
|---|--------|------|
| N1 | 单次任务完成时间 | ≤ 5 分钟（标准模式） |
| N2 | 系统可用性 | 连续稳定运行 24h 无崩溃 |
| N3 | 错误容忍 | 单个 Tool 失败不影响整体流程 |
| N4 | 容器化部署 | docker-compose up 一键启动 |

---

## 9. 附录

### 9.1 关键技术决策记录

| 决策 | 选项 | 选择 | 理由 |
|------|------|------|------|
| Agent 框架 | LangGraph vs. CrewAI vs. AutoGen | LangGraph | 最大的灵活性，图结构可控，面试价值最高 |
| 向量数据库 | ChromaDB vs. Pinecone vs. Qdrant | ChromaDB | 零依赖，本地运行，适合 Demo |
| 搜索 API | Tavily vs. SerpAPI vs. Bing | Tavily | 专为 LLM Agent 设计，API 简洁 |
| 前端框架 | React vs. Streamlit vs. Gradio | React | 更好的面试展示，Tailwind 快速实现 |
| 报告格式 | Markdown + WeasyPrint | Markdown+PDF | 兼顾可读性与正式输出 |

### 9.2 环境变量配置 (.env.example)

```bash
# LLM
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=sk-ant-xxx
LLM_MODEL_PLANNER=claude-sonnet-4-5
LLM_MODEL_RESEARCHER=claude-haiku-4-5
LLM_MODEL_WRITER=claude-sonnet-4-5
LLM_MODEL_REVIEWER=claude-sonnet-4-5

# Search
SEARCH_API_PROVIDER=tavily
TAVILY_API_KEY=tvly-xxx

# Redis
REDIS_URL=redis://localhost:6379

# ChromaDB
CHROMA_DB_PATH=./data/chroma_db

# Server
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info
```

### 9.3 docker-compose.yml

```yaml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379
      - CHROMA_DB_PATH=/app/data/chroma_db
    volumes:
      - ./data:/app/data
    depends_on:
      - redis
    env_file:
      - .env

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
```

### 9.4 项目亮点提炼（面试用）

1. **多 Agent 协作架构**：基于 LangGraph 设计 Planner-Researcher-Writer-Reviewer 四 Agent 系统，实现复杂研究任务的自动拆解、执行和质量闭环。引入 Reviewer 反射机制，通过迭代评分提升输出质量，最多可做 3 轮自动优化。

2. **插件式 Tool 系统**：设计统一的 Tool Router 路由层，Agent 按需动态调用搜索、网页浏览、Python 执行等工具。新增工具只需继承 BaseTool 并注册，零侵入扩展。

3. **RAG + 双记忆系统**：Session Memory（Redis）保存任务上下文实现多轮交互，Knowledge Memory（ChromaDB）持久化研究成果实现跨任务知识复用。RAG 检索增强减少 LLM 幻觉，确保报告有可靠引用来源。

---

### 9.5 近期新增功能 — 自定义 Skills 系统（2026-08）

- **技能模型**：SQLite `skills` 表，名称/描述/触发关键词/生效 Agent/指令正文/启用开关
- **触发注入**：任务文本命中关键词（大小写不敏感，空关键词恒生效）后，以「用户技能」章节注入对应 Agent system prompt
- **管理界面**：顶部「技能管理」弹窗，支持新建/编辑/启停/删除与触发匹配预览
- **内置示例**：首次启动播种 executive-summary、code-review-analysis、market-comparison

---

### 9.6 近期新增功能 — 用户特化 Agent（2026-08）

- **用户档案**：`user_profiles` 表，写作风格/领域偏好/模型覆盖/附加指令；未登录使用默认档案，登录自动建档
- **身份贯通**：研究任务携带档案，偏好以「用户偏好」章节注入全部 Agent
- **技能隔离**：`skills.owner_id` + `user_skill_prefs`，私有技能 + 全局技能启用开关
- **模型覆盖**：档案设置 preferred_model 后替换 Agent 默认模型

---

### 9.7 近期新增功能 — 自进化模块（2026-08）

- **任务复盘**：高分（≥0.75）或低分（≤0.45）任务完成后后台分析评审反馈，提炼经验
- **技能草稿**：LLM 生成技能提案（名称/描述/触发词/Agent/指令），存入 evolution_drafts 待确认
- **用户确认**：接受后生成真实技能（默认个人，可发布全局），拒绝即废弃
- **闭环**：执行 → 复盘 → 草稿 → 确认 → 技能生效

---

### 9.8 近期新增功能 — Embedding 模型配置（2026-08）

- LLM 配置页新增「Embedding 模型」，复用现有 API Key/Base URL（OpenAI 兼容）
- Embedder 优先读取已保存配置，配置变化后轻量重建、立即生效

---

*本文档版本：v1.0 | 最后更新：2026-07-19*
